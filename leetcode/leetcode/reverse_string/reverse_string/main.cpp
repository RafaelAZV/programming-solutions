#include <iostream>
#include <string>
#include <vector>
using namespace std;

std::vector<std::vector<string> > groupAnagrams(std::vector<std::vector<string> > &strs){

    std::vector<bool> visited (strs.size(), false);
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
